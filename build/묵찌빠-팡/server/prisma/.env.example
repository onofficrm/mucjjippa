# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Use `server/.env` (see `.env.example`). This file documents required keys for Prisma CLI.

DATABASE_URL=postgresql://localhost:5432/mucjjippa?schema=public
